specVersion: 0.0.3
description: Masterchef
repository: https://github.com/soulswapfinance/soulswap-subgraph
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: SoulSummoner
    network: {{ network }}
    source:
      address: '{{ address }}'
      abi: SoulSummoner
      startBlock: {{ startBlock }}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - SoulSummoner
        - History
        - Pool
        - PoolHistory
        - User
      abis:
        - name: SoulSummoner
          file: ./config/summoner.json
        - name: Factory
          file: ./config/factory.json
        - name: Pair
          file: ./config/pair.json
        - name: ERC20
          file: ./config/ERC20.json
      eventHandlers:
        - event: Deposit(indexed address,indexed uint256,uint256)
          handler: deposit
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: ownershipTransferred
        - event: Withdraw(indexed address,indexed uint256,uint256)
          handler: withdraw
      callHandlers:
        - function: add(uint256,address,bool)
          handler: add
        - function: set(uint256,uint256,bool)
          handler: set
        - function: massUpdatePools()
          handler: massUpdatePools
        - function: updatePool(uint256)
          handler: updatePool
        - function: setMigrator(address)
          handler: setMigrator
        - function: migrate(uint256)
          handler: migrate
      file: ./src/masterchef.ts
