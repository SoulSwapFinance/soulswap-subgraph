{
    "name": "user",
    "license": "MIT",
    "version": "1.0.0",
    "author": "SoulSwapFinance",
    "scripts": {
        "codegen": "graph codegen subgraph.yaml",
        "build": "graph build subgraph.yaml",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ soulswapfinance/user subgraph.yaml",
        "prepare": "mustache config/fantom.json template.yaml > subgraph.yaml"
    }
}